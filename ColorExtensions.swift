import SwiftUI

extension Color {
    // Background colors
    static let background = Color(red: 0.98, green: 0.98, blue: 0.98) // hsl(30 30% 98%)
    static let foreground = Color(red: 0.2, green: 0.2, blue: 0.22) // hsl(240 10% 20%)
    
    // Card colors
    static let card = Color.white
    static let cardForeground = Color(red: 0.2, green: 0.2, blue: 0.22)
    
    // Primary colors (coral)
    static let primary = Color(red: 0.98, green: 0.55, blue: 0.45) // hsl(15 90% 65%)
    static let primaryForeground = Color.white
    
    // Secondary colors
    static let secondary = Color(red: 0.85, green: 0.94, blue: 0.98) // hsl(200 60% 94%)
    static let secondaryForeground = Color(red: 0.2, green: 0.5, blue: 0.6) // hsl(200 60% 35%)
    
    // Muted colors
    static let muted = Color(red: 0.94, green: 0.93, blue: 0.92) // hsl(30 20% 94%)
    static let mutedForeground = Color(red: 0.5, green: 0.5, blue: 0.52) // hsl(240 5% 50%)
    
    // Accent colors (teal)
    static let accent = Color(red: 0.2, green: 0.65, blue: 0.55) // hsl(160 60% 45%)
    static let accentForeground = Color.white
    
    // Border
    static let border = Color(red: 0.9, green: 0.88, blue: 0.86) // hsl(30 20% 90%)
}

