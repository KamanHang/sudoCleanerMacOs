import AppKit
import Foundation

// This is a utility to help generate app icons from the SVG
// To use: Open the SVG in Preview, export as PNG at 1024x1024, then use that

class AppIconGenerator {
    static func generateIcons() {
        // This would generate icons programmatically
        // For now, manual conversion is needed
        print("To generate app icons:")
        print("1. Open sudocleaner-logo.svg in Preview or a graphics editor")
        print("2. Export at sizes: 16, 32, 64, 128, 256, 512, 1024")
        print("3. Place in Assets.xcassets/AppIcon.appiconset/")
    }
}

